% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{mce}
\alias{mce}
\title{Maximum Calibration Error}
\usage{
mce(p, y, bins = 10, type = c("classwise", "confidence"))
}
\arguments{
\item{p}{Predicted probabilities. A numeric vector in \verb{[0, 1]} for binary
problems, or a numeric matrix with one column per class for multiclass
problems. Matrix inputs must have finite entries in \verb{[0, 1]}, at least two
columns, and rows summing to one within absolute tolerance \code{1e-6}.}

\item{y}{Outcome labels. A vector coded as \code{0} and \code{1} for binary problems,
or a factor or vector of integer class codes in \code{1:K} for multiclass
problems.}

\item{bins}{Number of equal-width bins on \verb{[0, 1]}. Must be a single
positive integer.}

\item{type}{Multiclass aggregation, either \code{"classwise"} or \code{"confidence"}.
Ignored for binary inputs.}
}
\value{
A single numeric value.
}
\description{
\code{mce()} returns the largest empirical absolute gap between mean confidence
and empirical event frequency among non-empty equal-width bins. For
multiclass inputs the \code{"classwise"} form returns the largest binary MCE
across the one-vs-rest columns and the \code{"confidence"} form uses the
top-label confidence.
}
\details{
Using the same bin notation and endpoint convention as \code{\link[=ece]{ece()}}, the binary
empirical maximum calibration error is

\deqn{\operatorname{MCE} =
  \max_{b: n_b > 0}
  |\operatorname{acc}(b) - \operatorname{conf}(b)|.}{
MCE = max_{b: n_b > 0} |acc(b) - conf(b)|.}

Empty bins are ignored. For a multiclass probability matrix,
\code{type = "classwise"} returns the maximum of the one-vs-rest binary MCE values
across classes,

\deqn{\operatorname{MCE}_{\mathrm{cw}} =
  \max_{1 \le k \le K}
  \operatorname{MCE}(p_{\cdot k}, \mathbf{1}\{y_i = k\}).}{
MCE_cw = max_k MCE(p_.k, indicator(y_i = k)).}

\code{type = "confidence"} returns \eqn{\operatorname{MCE}(r, c)}{MCE(r, c)}
using the top-label confidence and correctness variables defined in \code{\link[=ece]{ece()}}.
}
\examples{
predictions <- data.frame(
  p = c(0.10, 0.20, 0.80, 0.90),
  y = c(0, 0, 1, 1)
)

predictions |>
  dplyr::summarise(mce = mce(p, y, bins = 2))
}
\references{
Guo, C., Pleiss, G., Sun, Y., & Weinberger, K. Q. (2017). On calibration of
modern neural networks. Proceedings of the 34th International Conference on
Machine Learning.
}
