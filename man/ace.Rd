% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{ace}
\alias{ace}
\title{Average Calibration Error}
\usage{
ace(p, y, bins = 10, type = c("classwise", "confidence"))
}
\arguments{
\item{p}{Predicted probabilities. A numeric vector in \verb{[0, 1]} for binary
problems, or a numeric matrix with one column per class for multiclass
problems. Matrix inputs must have finite entries in \verb{[0, 1]}, at least two
columns, and rows summing to one within absolute tolerance \code{1e-6}.}

\item{y}{Outcome labels. A vector coded as \code{0} and \code{1} for binary problems,
or a factor or vector of integer class codes in \code{1:K} for multiclass
problems.}

\item{bins}{Number of equal-width bins on \verb{[0, 1]}. Must be a single
positive integer.}

\item{type}{Multiclass aggregation, either \code{"classwise"} or \code{"confidence"}.
Ignored for binary inputs.}
}
\value{
A single numeric value.
}
\description{
\code{ace()} returns the empirical unweighted mean absolute calibration gap over
non-empty equal-width bins. Unlike \code{ece()}, each non-empty bin contributes
equally. For multiclass inputs the \code{"classwise"} form averages the binary ACE
over the one-vs-rest columns and the \code{"confidence"} form uses the top-label
confidence.
}
\details{
Using the same bin notation and endpoint convention as \code{\link[=ece]{ece()}}, let \eqn{M}
be the number of non-empty bins. The binary empirical average calibration
error is

\deqn{\operatorname{ACE} =
  \frac{1}{M}\sum_{b: n_b > 0}
  |\operatorname{acc}(b) - \operatorname{conf}(b)|.}{
ACE = (1 / M) sum_{b: n_b > 0} |acc(b) - conf(b)|.}

Unlike ECE, ACE does not weight bins by their sample sizes. Sparse bins and
dense bins therefore contribute equally once they are non-empty. This
implementation uses equal-width bins on \verb{[0, 1]}; it does not construct
adaptive or equal-frequency bins. For a multiclass probability matrix,
\code{type = "classwise"} returns the arithmetic mean of the one-vs-rest binary
ACE values,

\deqn{\operatorname{ACE}_{\mathrm{cw}} =
  \frac{1}{K}\sum_{k = 1}^K
  \operatorname{ACE}(p_{\cdot k}, \mathbf{1}\{y_i = k\}).}{
ACE_cw = K^{-1} sum_k ACE(p_.k, indicator(y_i = k)).}

\code{type = "confidence"} returns \eqn{\operatorname{ACE}(r, c)}{ACE(r, c)}
using top-label confidence and correctness.
}
\examples{
predictions <- data.frame(
  p = c(0.10, 0.20, 0.80, 0.90),
  y = c(0, 0, 1, 1)
)

predictions |>
  dplyr::summarise(ace = ace(p, y, bins = 2))
}
\references{
Niculescu-Mizil, A., & Caruana, R. (2005). Predicting good probabilities
with supervised learning. Proceedings of the 22nd International Conference
on Machine Learning.
}
